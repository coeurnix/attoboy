# String

## Обзор

Класс `attoboy::String` — это основной текстовый тип в библиотеке attoboy. Он представляет собой **неизменяемую UTF-8 строку** с богатым набором операций для инспекции, поиска, преобразования, разбиения/объединения и форматирования. Он спроектирован так, чтобы быть:

* **Удобным в использовании**: конструкторы принимают многие распространённые типы (булевы значения, числа, контейнеры).
* **Безопасным по умолчанию**: операции, основанные на символах, уважают границы UTF-8 кодовых точек.
* **Практичным**: интеграция с `List`, `Map` и `Set` позволяет выполнять простые преобразования в JSON-стиле и шаблонное форматирование.
* **Дружелюбным к компактным бинарным сборкам**: работает без стандартной библиотеки C++ или CRT.

На высоком уровне `String` можно рассматривать как замену `std::string` в attoboy, но с:

* **Поддержкой UTF-8** (символы против байтов).
* **Неизменяемостью**: после создания содержимое не меняется.
* Встроенными преобразованиями из и в многие типы attoboy.

### Неизменяемость и семантика значения

`String` является **неизменяемым**: ни одна из его функций-членов не изменяет содержимое существующего объекта строки. Вместо этого любая операция, которая «изменяет» строку, возвращает **новую** `String` с преобразованным содержимым.

Например:

```cpp
using namespace attoboy;

String original("hello");
String upper = original.upper();

// original is still "hello"
// upper is "HELLO"
```

Эта неизменяемость имеет несколько преимуществ:

* Вы можете безопасно разделять экземпляры `String` между разными частями программы (и между потоками) без опасений случайной модификации.
* Функциям, получающим параметр `String`, не нужно делать оборонительную копию; они знают, что значение не изменится.
* Внутренне реализация может оптимизировать использование памяти, разделяя базовое хранилище при копировании или присваивании.

### UTF-8, символы и байты

Библиотека везде использует **UTF-8 кодировку**. Это имеет два практических следствия:

1. Один видимый пользователю символ (Unicode **кодовая точка**) может занимать **несколько байтов**.
2. Некоторые операции определены в терминах **символов** (кодовых точек), в то время как другие работают с **сырыми байтами**.

API `String` предоставляет оба представления:

* **На уровне символов (учитывая UTF-8)**:

  * `length()`
  * `at()`
  * `substring()`
  * `getPositionOf()`
  * `trim()`, `upper()`, `lower()`, `reverse()` (для символов)
* **На уровне байтов (сырые данные)**:

  * `byteLength()`
  * `byteAt()`
  * `byteSubstring()`
  * `FromCStr()` (использует сырую длину в байтах)

При работе с читаемым текстом (интерфейс пользователя, логи и т.д.) почти всегда следует использовать операции на уровне **символов**. Версии на уровне байтов применяйте, когда вы имеете дело с протоколами или бинарными кодировками, где важны точные позиции байтов.

### Индексация и отрицательные индексы

Несколько функций `String` принимают **индексы** (позиции) или **диапазоны**:

* Индексы — **с нуля**, где `0` означает первый символ (или байт).
* Некоторые функции явно поддерживают **отрицательные индексы** для подсчёта с конца:

  * `at(int index)`
  * `byteAt(int index)`
  * `substring(int start, int end = -1)`

Для этих функций:

* `-1` означает «последний символ».
* `-2` означает «предпоследний» и т.д.

Для `substring(start, end)`:

* `start` и `end` — индексы символов.
* `end` является **исключающим** (символ на позиции `end` не включается).
* Если `end` равен `-1` (по умолчанию), подстрока простирается до конца строки.

### Интеграция с другими типами attoboy

Класс `String` тесно взаимодействует с другими основными типами attoboy:

* `String(const List &list)` и `String(const Set &set)` создают JSON-массивы.
* `String(const Map &map)` создаёт JSON-объект.
* `format(const List &list)` и `format(const Map &map)` заменяют заполнители вроде `{0}` или `{key}`.
* `lines()`, `split()` и `join()` интегрируются с `List` для простой токенизации и реконструкции.

Это облегчает перенос текста между структурами данных, сериализацию в JSON и построение шаблонных сообщений без дополнительных библиотек.

---

## Справочник

Каждая запись ниже охватывает один публичный конструктор, метод, статическую функцию или оператор `attoboy::String`. Для каждого вы найдёте:

* **Подпись** — точное объявление из заголовочного файла.
* **Краткое описание** — исходная однострочная документация Doxygen.
* Описания **параметров** и **возвращаемого значения**.
* **Детали** — дополнительные сведения, оговорки и короткий пример.

> **Примечание:** Все примеры предполагают `using namespace attoboy;`.

---

#### `String()`

**Подпись**

```cpp
String();
```

**Краткое описание**
Создаёт пустую строку.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор по умолчанию создаёт `String`, который не содержит символов и байтов. Для такой строки:

* `length()` возвращает `0`.
* `byteLength()` возвращает `0`.
* `isEmpty()` возвращает `true`.
* `c_str()` возвращает указатель на один байт `'\0'`.

Пустые строки являются естественным значением по умолчанию и распространённой отправной точкой для построения новых строк с помощью конкатенации и других операций.

**Пример**

```cpp
String s;
String message = s.append("Hello");  // message == "Hello"
```

*В этом примере начинается с пустой `String` и формируется новая путём добавления текста.*

---

#### `String(const char *str)`

**Подпись**

```cpp
String(const char *str);
```

**Краткое описание**
Создаёт строку из null-терминированной C-строки.

**Параметры**

* `str` – Указатель на null-терминированную последовательность байтов в кодировке UTF-8.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор является основным мостом от сырых C-строк к `attoboy::String`. Содержимое копируется во внутреннее представление, поэтому вы можете безопасно изменять или освобождать исходный буфер после создания `String`.

Ключевые моменты:

* Вход должен быть **null-терминирован**.
* Байты интерпретируются как UTF-8. Если последовательность байтов не является корректной UTF-8, операции на уровне символов могут работать неожиданно.
* `nullptr` обычно трактуется как пустая строка; чтобы избежать неоднозначности, по возможности передавайте валидный указатель.

**Пример**

```cpp
const char *raw = "Hello, UTF-8 🌍";
String s(raw);
int chars = s.length();      // character count (including emoji)
int bytes = s.byteLength();  // raw byte count
```

*В этом примере C-строка в кодировке UTF-8 оборачивается в неизменяемую `String` и проводится сравнение количества символов и байтов.*

---

#### `String(bool value)`

**Подпись**

```cpp
String(bool value);
```

**Краткое описание**
Создаёт строку из булева значения ("true" или "false").

**Параметры**

* `value` – Булево значение для преобразования.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор преобразует булево значение в текстовую литералу `"true"` или `"false"` (все строчные). Полезно при логировании, форматировании сообщений или при формировании JSON без ручной сборки строк.

**Пример**

```cpp
bool enabled = true;
String text(enabled);   // "true"
String msg = String("Feature enabled = ") + text;
```

*В этом примере булев флаг конвертируется в читаемую строку и конкатенируется в сообщение.*

---

#### `String(char value)`

**Подпись**

```cpp
String(char value);
```

**Краткое описание**
Создаёт строку из одного символа.

**Параметры**

* `value` – Один байт, который будет сохранён как однобайтовая строка.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор создаёт `String`, содержащую ровно один байт. Для ASCII-символов это соответствует одному символу. Для общей Unicode-текстов рекомендуют конструировать из UTF-8 `const char *` или комбинировать существующие `String`-объекты.

**Пример**

```cpp
String exclamation('!');
String word("Go");
String sentence = word + exclamation;  // "Go!"
```

*В этом примере строится слово, а затем добавляется односимвольная `String`, созданная из `char`.*

---

#### `String(int value)`

**Подпись**

```cpp
String(int value);
```

**Краткое описание**
Создаёт строку из целого числа.

**Параметры**

* `value` – 32-битное целое для преобразования.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор преобразует целое число в его десятичное представление (например, `42` → `"42"`, `-5` → `"-5"`). Разделителей тысяч или дополнительного форматирования нет.

**Пример**

```cpp
int score = 42;
String scoreText(score);  // "42"
String label = String("Score: ") + scoreText;  // "Score: 42"
```

*В этом примере целое число счёта преобразуется в текст и конкатенируется в метку.*

---

#### `String(long long value)`

**Подпись**

```cpp
String(long long value);
```

**Краткое описание**
Создаёт строку из 64-битного целого.

**Параметры**

* `value` – 64-битное целое для преобразования.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Ведёт себя аналогично `String(int)`, но поддерживает полный диапазон `long long`. Используйте его, когда нужно преобразовать в строку большие счётчики, метки времени или размеры, которые не помещаются в 32-битное целое.

**Пример**

```cpp
long long totalBytes = 1048576;
String bytesText(totalBytes);  // "1048576"
```

*В этом примере 64-битное значение (например, размер в байтах) преобразуется в текст для отчёта.*

---

#### `String(float value)`

**Подпись**

```cpp
String(float value);
```

**Краткое описание**
Создаёт строку из числа с плавающей точкой (float).

**Параметры**

* `value` – Значение с плавающей точкой для преобразования.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Преобразует `float` в текстовое десятичное представление. Точное форматирование (количество знаков после запятой, завершающие нули) следует внутренним правилам библиотеки, но можно рассчитывать на обычное читаемое десятичное представление.

**Пример**

```cpp
float ratio = 0.5f;
String text(ratio);  // e.g., "0.5"
String label = String("Ratio: ") + text;
```

*В этом примере число с плавающей точкой преобразуется в простую строку.*

---

#### `String(const List &list)`

**Подпись**

```cpp
String(const List &list);
```

**Краткое описание**
Создаёт JSON-массив из списка (например, [1,"a",true]).

**Параметры**

* `list` – `List`, элементы которого будут сериализованы в JSON.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор рендерит `List` как JSON-массив, где каждый элемент сериализуется в зависимости от его типа:

* Числа как JSON-числа.
* Булевы значения как `true`/`false`.
* Строки как заключённые в кавычки, экранированные JSON-строки.
* Вложенные `List`, `Map` и `Set` становятся вложенными массивами/объектами.

Это особенно полезно для логирования структурированных данных или отправки простых JSON-пейлоадов без полноценной JSON-библиотеки.

**Пример**

```cpp
List values;
values.append(1).append("two").append(true);

String json(values);  // e.g., "[1,\"two\",true]"
```

*В этом примере разнородный список конвертируется в строку JSON-массива.*

---

#### `String(const Map &map)`

**Подпись**

```cpp
String(const Map &map);
```

**Краткое описание**
Создаёт JSON-объект из карты (например, {"k":"v"}).

**Параметры**

* `map` – `Map`, пары ключ–значение которого будут сериализованы в JSON.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор рендерит `Map` как JSON-объект. Ключи и значения преобразуются в JSON; ключи обычно являются строками, но другие типы преобразуются в строки по мере необходимости.

Ключи должны быть уникальны в карте; результирующий JSON использует эти ключи как имена свойств объекта.

**Пример**

```cpp
Map user;
user.put("name", "Alice").put("age", 30);

String json(user);  // e.g., "{\"name\":\"Alice\",\"age\":30}"
```

*В этом примере карта свойств конвертируется в строку JSON-объекта.*

---

#### `String(const Set &set)`

**Подпись**

```cpp
String(const Set &set);
```

**Краткое описание**
Создаёт JSON-массив из множества.

**Параметры**

* `set` – `Set`, значения которого будут сериализованы в JSON.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот конструктор рендерит `Set` как JSON-массив, аналогично `String(const List &list)`. Поскольку множества неупорядочены, порядок в результирующем JSON-массиве не гарантируется.

Дубликаты уже исключены самим `Set`.

**Пример**

```cpp
Set tags;
tags.put("red").put("blue").put("green");

String json(tags);  // e.g., "[\"red\",\"blue\",\"green\"]"
```

*В этом примере множество строк конвертируется в JSON-массив.*

---

#### `template <typename T, typename U, typename... Args> String(const T &first, const U &second, const Args &...rest)`

**Подпись**

```cpp
template <typename T, typename U, typename... Args>
String(const T &first, const U &second, const Args &...rest)
    : String(String(first) + String(second) + String(rest...)) {}
```

**Краткое описание**
Конкатенирует несколько значений в одну строку.

**Параметры**

* `first` – Первое значение для преобразования в `String`.
* `second` – Второе значение для преобразования в `String`.
* `rest` – Ноль или более дополнительных значений для преобразования и добавления.

Каждый аргумент может быть любого типа, конструктивного как `String`, например `const char *`, `String`, `int`, `bool`, `List` и т.д.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Этот вариативный конструктор предоставляет лаконичный способ построить `String` из последовательности разнородных значений. Внутри каждый аргумент преобразуется в `String` и конкатенируется в порядке вызова.

Можно мыслить о нём как о:

```cpp
String(String(first)) + String(second) + String(rest)...
```

Это особенно удобно для формирования сообщений или логов без ручного чередования `+`.

**Пример**

```cpp
int id = 7;
String status = "OK";

String line("Task #", id, " status: ", status);
// e.g., "Task #7 status: OK"
```

*В этом примере сообщение строится из смеси литералов, целых чисел и других строк с помощью вариативного конструктора.*

---

#### `String(const String &other)`

**Подпись**

```cpp
String(const String &other);
```

**Краткое описание**
Создаёт копию другой строки.

**Параметры**

* `other` – Существующая `String` для копирования.

**Возвращаемое значение**

* *(конструктор; не применяется)*

**Детали**

Конструктор копирования создаёт новую `String` с тем же содержимым, что и `other`. Поскольку `String` неизменяем, копии концептуально независимы и безопасны для совместного использования. Внутренняя реализация может разделять хранилище для эффективности.

**Пример**

```cpp
String original("hello");
String copy(original);
// both represent "hello"
```

*В этом примере демонстрируется копирование строкового значения.*

---

#### `~String()`

**Подпись**

```cpp
~String();
```

**Краткое описание**
Уничтожает строку и освобождает память.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* *(деструктор; не применяется)*

**Детали**

Когда `String` выходит из области видимости или иным образом уничтожается, его деструктор освобождает любые внутренние ресурсы (например, буферы в куче). Поскольку `String` использует RAII, вам не нужно вручную освобождать память.

**Пример**

```cpp
{
  String temp("temporary");
  // temp is valid here
}
// temp is destroyed here; memory is released automatically
```

*В этом примере показано автоматическое освобождение `String`, когда он выходит из области видимости.*

---

#### `static String FromCStr(const char *data, int size, const String &encoding = "utf-8")`

**Подпись**

```cpp
static String FromCStr(const char *data, int size, const String &encoding = "utf-8");
```

**Краткое описание**
Создаёт строку из сырых байтов с заданной длиной (не требует null-терминации). encoding по умолчанию "utf-8".

**Параметры**

* `data` – Указатель на буфер, содержащий байты в указанной `encoding`.
* `size` – Число байтов для копирования из `data`.
* `encoding` – Название кодировки входных данных. По умолчанию `"utf-8"`. Байты конвертируются во внутреннее UTF-8 представление `String`.

**Возвращаемое значение**

* `String`, содержащая конвертированный текст ровно из `size` байтов из `data`.

**Детали**

В отличие от конструктора `const char *`, эта статическая функция **не** требует завершающего `'\0'` в исходном буфере. Она идеальна, когда:

* У вас есть буфер, который может содержать встроенные нули.
* Вы работаете с подстрокой большего буфера, не имеющей отдельного null-терминатора.
* Вы получаете данные в известной кодировке и нужно сконвертировать их во внутренний UTF-8.

Параметр `encoding` позволяет указать, как интерпретировать входные байты. Если он опущен, байты предполагаются уже в UTF-8. Если вы укажете другую кодировку, например `"ansi"` или `"cp1252"`, функция попытается конвертировать из этой кодировки в UTF-8. Если входные байты невалидны для заданной кодировки, поведение зависит от реализации конвертации (например, некорректные последовательности могут быть заменены или привести к ошибке).

Когда вы планируете использовать операции на уровне символов, такие как `length()`, `substring()` или `at()`, убедитесь, что:

* `size` и `encoding` описывают полные символы (не разрезают много-байтовую последовательность).
* Исходный буфер действительно содержит текст в указанной кодировке.

**Пример**

```cpp
const char raw[] = "Hello, world!";
String partial = String::FromCStr(raw, 5);  // "Hello"

// Explicitly specifying encoding (input already in UTF-8)
String utf8 = String::FromCStr(raw, sizeof(raw) - 1, "utf-8");
```
*В этом примере создаётся `String` из первых 5 байтов C-буфера и полная версия в UTF-8.*

---

#### `String &operator=(const String &other)`

**Подпись**

```cpp
String &operator=(const String &other);
```

**Краткое описание**
Присваивает этой строке значение другой строки.

**Параметры**

* `other` – Строка, значение которой должно быть присвоено.

**Возвращаемое значение**

* Ссылка на `*this`, позволяющая цепочку присваиваний.

**Детали**

Оператор копирующего присваивания заменяет содержимое левой стороны на содержимое `other`. Поскольку `String` неизменяем, операция просто делает переменную ссылаться на то же логическое значение, что и `other`.

Самоприсваивание безопасно; типичные реализации обнаруживают и эффективно обрабатывают его.

**Пример**

```cpp
String name("Alice");
String other("Bob");

name = other;  // name now represents "Bob"
```

*В этом примере переменной `String` присваивается значение другой строки.*

---

#### `int length() const`

**Подпись**

```cpp
int length() const;
```

**Краткое описание**
Возвращает количество UTF-8 символов в строке.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Число Unicode кодовых точек в строке (может быть меньше, чем `byteLength()`).

**Детали**

`length()` считает **символы**, а не байты. Для простого ASCII текста `length()` и `byteLength()` равны. Для не-ASCII символов (например, эмодзи или нелатинских скриптов) один символ может храниться в нескольких байтах, поэтому `byteLength()` может быть больше.

Используйте `length()`, когда важны видимые пользователю символы: позиции курсора, правила валидации вроде «не более 80 символов» и т.п.

**Пример**

```cpp
String ascii("hello");
String unicode("🌍");  // earth emoji

int la = ascii.length();    // 5
int ba = ascii.byteLength(); // 5

int lu = unicode.length();   // 1 (one character)
int bu = unicode.byteLength(); // > 1 bytes
```

*В этом примере показано различие между количеством символов и байтов для ASCII и Unicode текста.*

---

#### `int byteLength() const`

**Подпись**

```cpp
int byteLength() const;
```

**Краткое описание**
Возвращает количество байтов в строке.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Количество байтов во внутреннем UTF-8 представлении строки (без завершающего нуля).

**Детали**

`byteLength()` даёт сырой размер хранения содержимого строки. Используйте его, когда:

* Записываете данные в бинарные протоколы, где требуются байтовые счётчики.
* Выделяете буферы для копирования UTF-8 данных.

Обратите внимание, что `byteLength()` может быть больше, чем `length()`, если строка содержит не-ASCII символы.

**Пример**

```cpp
String s("Hello, 🌍");
int bytes = s.byteLength();  // raw UTF-8 bytes
```

*В этом примере получается длина в байтах UTF-8 текста.*

---

#### `bool isEmpty() const`

**Подпись**

```cpp
bool isEmpty() const;
```

**Краткое описание**
Возвращает true, если строка пустая.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* `true`, если строка не содержит символов, иначе `false`.

**Детали**

Это удобная функция, эквивалентная проверке `length() == 0`. Часто более ясна и выразительна при проверке пустого ввода или необязательных полей.

**Пример**

```cpp
String name;

if (name.isEmpty()) {
  name = "Anonymous";
}
```

*В этом примере при пустой строке присваивается имя по умолчанию.*

---

#### `const char *c_str() const`

**Подпись**

```cpp
const char *c_str() const;
```

**Краткое описание**
Возвращает указатель на null-терминированную C-строку.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Указатель на null-терминированную UTF-8 последовательность байтов, управляемую `String`.

**Детали**

Эта функция предоставляет только для чтения C-представление внутренних данных. Возвращённый указатель:

* Остаётся валидным, пока жив объект `String`.
* Не должен модифицироваться.
* Может быть передан C-API, ожидающему `const char *`.

Поскольку `String` неизменяем, вызовы других методов не изменяют буфер для данного объекта.

**Пример**

```cpp
String s("Hello");
const char *p = s.c_str();
// p can be passed to C-style APIs that expect a const char*.
```

*В этом примере получается C-указатель для взаимодействия с API, не знакомыми с `String`.*

---

#### `char *c_str_allocated(const String &encoding = "utf-8") const`

**Подпись**

```cpp
char *c_str_allocated(const String &encoding = "utf-8") const;
```

**Краткое описание**
Возвращает выделенную в куче C-строку в указанной кодировке. Вызыватель обязан освободить указатель через Free(). encoding по умолчанию "utf-8".

**Параметры**

* `encoding` – Название желаемой выходной кодировки. По умолчанию `"utf-8"`.

**Возвращаемое значение**

* Указатель на вновь выделенную null-терминированную C-строку, содержащую текст в запрошенной кодировке. Вы обязаны освободить эту память вызовом `Free()` после использования.

**Детали**

Если `c_str()` выдаёт указатель во внутренний буфер `String`, то `c_str_allocated()` создаёт **отдельную копию в куче** содержимого в виде C-строки. Это полезно, когда нужно передать владение буфером в API, которое ожидает хранить `char *` дольше жизни `String`.

Ключевые моменты:

* Возвращённый указатель выделяется с помощью собственного механизма аллокации attoboy.
* Вы **должны** освободить его вызовом `Free(ptr)`, когда буфер больше не нужен.
* Параметр `encoding` контролирует, как внутренний UTF-8 текст конвертируется в выходные байты. При значении по умолчанию `"utf-8"` вы получаете UTF-8 C-строку. Другие названия кодировок могут поддерживаться в зависимости от возможностей конвертации.
* Если некоторые символы не представимы в запрошенной кодировке, конвертация может заменить их или провалиться, в зависимости от реализации.

Поскольку функция выполняет выделение и (опционально) конвертацию кодировки, она может быть дороже, чем простой вызов `c_str()`. Используйте её только когда действительно нужен независимый, управляемый вызывающим буфер.

**Пример**

```cpp
String s("Hello, world");

// Get a UTF-8 C string that the caller owns
char *buffer = s.c_str_allocated();

// Use buffer with a C API that stores the pointer
some_c_api_using_string(buffer);

// When finished with the buffer, you MUST free it with Free()
Free(buffer);
```

*В этом примере получается выделенная C-строка, она передаётся C-API, а затем корректно освобождается через `Free()`.*

---

#### `String at(int index) const`

**Подпись**

```cpp
String at(int index) const;
```

**Краткое описание**
Возвращает символ по индексу в виде строки. Отрицательные индексы считаются с конца.

**Параметры**

* `index` – Индекс символа (0-based). Отрицательные значения считаются с конца.

**Возвращаемое значение**

* `String`, содержащая ровно один символ (одну Unicode кодовую точку).

**Детали**

`at()` возвращает символ на позиции `index` как новую односимвольную строку. Он соблюдает границы UTF-8 кодовых точек, поэтому никогда не возвращает половину много-байтового символа.

Поддерживаются отрицательные индексы:

* `-1` – последний символ.
* `-2` – предпоследний, и т.д.

Если `index` выходит за диапазон, поведение определяется реализацией; на практике следует удостовериться, что `0 <= index < length()` (или соответствующий отрицательный диапазон) перед вызовом.

**Пример**

```cpp
String text("Hello");
String first = text.at(0);   // "H"
String last  = text.at(-1);  // "o"
```

*В этом примере извлекаются первый и последний символы строки.*

---

#### `String byteAt(int index) const`

**Подпись**

```cpp
String byteAt(int index) const;
```

**Краткое описание**
Возвращает один байт по индексу в виде строки. Отрицательные индексы считаются с конца.

**Параметры**

* `index` – Индекс байта (0-based). Отрицательные значения считаются с конца байтовой последовательности.

**Возвращаемое значение**

* `String`, содержащая ровно один байт.

**Детали**

`byteAt()` предоставляет доступ на уровне байтов. Это в основном полезно для отладки или низкоуровневой обработки; большая часть логики работы с текстом должна использовать `at()`.

Если строка содержит много-байтовые символы, `byteAt()` может вернуть не-ASCII байт продолжения, который не имеет смысла как отдельный символ.

**Пример**

```cpp
String text("A");
String b0 = text.byteAt(0);   // "A"
```

*В этом примере читается первый байт простой ASCII-строки.*

---

#### `String substring(int start, int end = -1) const`

**Подпись**

```cpp
String substring(int start, int end = -1) const;
```

**Краткое описание**
Возвращает подстроку от start до end (исключая end). Отрицательные индексы считаются с конца.

**Параметры**

* `start` – Индекс символа первого включаемого символа. Может быть отрицательным для обращения с конца.
* `end` – Индекс символа **после** последнего включаемого (исключающий). `-1` означает «до конца строки».

**Возвращаемое значение**

* Новая `String`, содержащая указанный диапазон символов.

**Детали**

`substring()` оперирует на уровне **символов**, а не байтов. Он уважает границы UTF-8, поэтому можно безопасно вырезать текст, содержащий много-байтовые символы.

Распространённые шаблоны:

* `substring(0, n)` – первые `n` символов.
* `substring(0)` или `substring(0, -1)` – полная копия.
* `substring(-n)` – последние `n` символов.

Необходимо удостовериться, что эффективный диапазон находится в пределах `[0, length()]`. Передача индексов вне допустимого диапазона даёт поведение, определяемое реализацией.

**Пример**

```cpp
String text("Hello, world");
String hello = text.substring(0, 5);  // "Hello"
String world = text.substring(7);     // "world"
```

*В этом примере извлекаются две подстроки с помощью start/end индексов.*

---

#### `String byteSubstring(int start, int end = -1) const`

**Подпись**

```cpp
String byteSubstring(int start, int end = -1) const;
```

**Краткое описание**
Возвращает подстроку на уровне байтов от start до end (исключая end).

**Параметры**

* `start` – Байтовый индекс первого включаемого байта.
* `end` – Байтовый индекс **после** последнего включаемого байта (исключающий). `-1` означает «до конца байтовой последовательности».

**Возвращаемое значение**

* Новая `String`, содержащая указанный диапазон байтов.

**Детали**

`byteSubstring()` работает в терминах сырых байтов, а не символов. Если вы разрежете в середине много-байтового символа, результат может содержать некорректный UTF-8. Используйте эту функцию только при намеренной работе с байтовыми данными (например, фрейминг протоколов, ручная кодировка).

**Пример**

```cpp
String raw("abcdef");
String mid = raw.byteSubstring(2, 4);  // "cd"
```

*В этом примере берётся двухбайтовый срез из простой ASCII-строки.*

---

#### `String duplicate() const`

**Подпись**

```cpp
String duplicate() const;
```

**Краткое описание**
Возвращает копию этой строки.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Новая `String`, содержащая тот же текст, что и оригинал.

**Детали**

`duplicate()` — это явный способ получить копию `String`. Поскольку `String` неизменяем, копирование в основном используется для ясности, когда вы хотите подчеркнуть, что работаете с независимым значением, даже несмотря на то, что модификации всегда возвращают новые строки.

**Пример**

```cpp
String original("hello");
String copy = original.duplicate();
// original and copy both represent "hello"
```

*В этом примере явно получается дубликат строкового значения.*

---

#### `bool contains(const String &sub) const`

**Подпись**

```cpp
bool contains(const String &sub) const;
```

**Краткое описание**
Возвращает true, если эта строка содержит подстроку.

**Параметры**

* `sub` – Подстрока для поиска.

**Возвращаемое значение**

* `true`, если `sub` встречается хотя бы один раз в этой строке; иначе `false`.

**Детали**

`contains()` — удобство для проверки наличия подстроки. Обычно выполняет прямой поиск по символам или байтам (зависит от реализации) и не использует регулярные выражения.

Если `sub` пустая, поведение определяется реализацией; в большинстве случаев пустая подстрока считается «присутствующей» в позиции 0, но лучше избегать передачи пустых строк.

**Пример**

```cpp
String text("Hello, world");
bool hasWorld = text.contains("world");  // true
```

*В этом примере проверяется, содержит ли строка заданное слово.*

---

#### `bool startsWith(const String &sub) const`

**Подпись**

```cpp
bool startsWith(const String &sub) const;
```

**Краткое описание**
Возвращает true, если эта строка начинается с подстроки.

**Параметры**

* `sub` – Кандидат на префикс.

**Возвращаемое значение**

* `true`, если `sub` совпадает с началом этой строки; иначе `false`.

**Детали**

`startsWith()` часто используется для определения протоколов, проверки путей и простого маршрутизации по префиксу. Сопоставление точное и чувствительно к регистру.

**Пример**

```cpp
String path("/api/users");
bool isApi = path.startsWith("/api/");  // true
```

*В этом примере проверяется, имеет ли путь определённый префикс.*

---

#### `bool endsWith(const String &sub) const`

**Подпись**

```cpp
bool endsWith(const String &sub) const;
```

**Краткое описание**
Возвращает true, если эта строка заканчивается указанной подстрокой.

**Параметры**

* `sub` – Кандидат на суффикс.

**Возвращаемое значение**

* `true`, если `sub` совпадает с окончанием этой строки; иначе `false`.

**Детали**

`endsWith()` полезен для проверки расширений файлов, простых шаблонов и других логик, основанных на суффиксах. Сопоставление точное и чувствительно к регистру.

**Пример**

```cpp
String filename("report.txt");
bool isTxt = filename.endsWith(".txt");  // true
```

*В этом примере проверяется расширение файла с помощью `endsWith()`.*

---

#### `int count(const String &sub) const`

**Подпись**

```cpp
int count(const String &sub) const;
```

**Краткое описание**
Возвращает число неперекрывающихся вхождений подстроки.

**Параметры**

* `sub` – Подстрока для подсчёта.

**Возвращаемое значение**

* Количество неперекрывающихся вхождений `sub` в этой строке.

**Детали**

`count()` проходит по строке и увеличивает счётчик каждый раз, когда находит вхождение `sub`, не перекрывающееся с предыдущим. Например, подсчёт `"aa"` в `"aaaa"` возвращает 2 (`"aa" + "aa"`).

Если `sub` пустая, результат не определён; избегайте передачи пустых подстрок.

**Пример**

```cpp
String text("one, two, three, two");
int twos = text.count("two");  // 2
```

*В этом примере подсчитывается, сколько раз слово встречается в предложении.*

---

#### `int getPositionOf(const String &sub, int start = 0) const`

**Подпись**

```cpp
int getPositionOf(const String &sub, int start = 0) const;
```

**Краткое описание**
Возвращает индекс символа подстроки или -1, если не найдено.

**Параметры**

* `sub` – Подстрока для поиска.
* `start` – Индекс символа, с которого должен начинаться поиск (по умолчанию `0`).

**Возвращаемое значение**

* Индекс символа первого совпадения на или после `start`, либо `-1`, если `sub` не найден.

**Детали**

Эта функция находит **первое вхождение** `sub`, начиная с указанной позиции в символах. Она возвращает позиции в терминах символов (не байтов), что удобно сочетать с `substring()`.

Если нужно найти все вхождения, можно выполнять цикл, передавая `start` как последний найденный индекс плюс длина `sub`.

**Пример**

```cpp
String text("banana");
int pos = text.getPositionOf("na");  // 2
int next = text.getPositionOf("na", pos + 2);  // 4
```

*В этом примере находятся первое и второе вхождения `"na"` в строке `"banana"`.*

---

#### `bool isNumber() const`

**Подпись**

```cpp
bool isNumber() const;
```

**Краткое описание**
Возвращает true, если строка является допустимым целым или десятичным числом.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* `true`, если содержимое может быть распознано как число; иначе `false`.

**Детали**

`isNumber()` проверяет, можно ли интерпретировать строку как:

* Целое число (например, `"42"`, `"-7"`), или
* Десятичное число с плавающей точкой (например, `"3.14"`, `"0.5"`).

Используйте это перед вызовами `toInteger()` или `toFloat()`, если нужно отличить действительное числовое `0` от недопустимой строки, которая также могла бы вернуть `0` по умолчанию.

**Пример**

```cpp
String s("123");
if (s.isNumber()) {
  int n = s.toInteger();  // 123
}
```

*В этом примере выполняется проверка, является ли строка числом, перед конвертацией.*

---

#### `int toInteger() const`

**Подпись**

```cpp
int toInteger() const;
```

**Краткое описание**
Преобразует строку в целое число. Возвращает 0 при ошибке.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Распарсенное целое значение, или `0`, если строка не является допустимым целым.

**Детали**

`toInteger()` пытается распарсить содержимое как десятичное целое (опционально со знаком). Если парсинг не удался, возвращается `0`. Это означает, что нельзя отличить `"0"` от недопустимого текста, используя только `toInteger()`; используйте `isNumber()` заранее, если нужно обнаруживать некорректный ввод.

**Пример**

```cpp
String s("42");
int value = s.toInteger();  // 42

String bad("abc");
int fallback = bad.toInteger();  // 0
```

*В этом примере показаны успешное и запасное целочисленные преобразования.*

---

#### `float toFloat() const`

**Подпись**

```cpp
float toFloat() const;
```

**Краткое описание**
Преобразует строку в float. Возвращает 0.0 при ошибке.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Распарсенное значение с плавающей точкой, или `0.0f`, если строка не является допустимым числом.

**Детали**

`toFloat()` парсит строку как десятичное число с плавающей точкой. При неудаче возвращается `0.0f`. Как и с `toInteger()`, используйте `isNumber()`, чтобы отличить валидный `0.0` от неверного текста.

**Пример**

```cpp
String s("3.5");
float value = s.toFloat();  // 3.5f
```

*В этом примере десятичный текст преобразуется в `float`.*

---

#### `bool toBool() const`

**Подпись**

```cpp
bool toBool() const;
```

**Краткое описание**
Возвращает true для "true", "yes", "1", "on" (независимо от регистра).

**Параметры**

* *(нет)*

**Возвращаемое значение**

* `true`, если текст совпадает с одним из распознаваемых значений истины; иначе `false`.

**Детали**

Этот помощник интерпретирует множество распространённых пользовательских вводов как булевы значения:

* Распознаваемые **истинные** значения (без учёта регистра): `"true"`, `"yes"`, `"1"`, `"on"`.
* Любое другое значение, включая `"false"`, `"no"`, `"0"` и пустые строки, даёт `false`.

Это полезно для значений конфигурации или аргументов командной строки, принимающих гибкие булевы значения.

**Пример**

```cpp
String flag("YES");
bool enabled = flag.toBool();  // true
```

*В этом примере строка в любом регистре интерпретируется как булево значение.*

---

#### `String append(const String &s) const`

**Подпись**

```cpp
String append(const String &s) const;
```

**Краткое описание**
Возвращает новую строку с добавленной подстрокой.

**Параметры**

* `s` – Текст, который будет добавлен в конец этой строки.

**Возвращаемое значение**

* Новая `String`, равная `(*this + s)`.

**Детали**

`append()` эквивалентен `operator+`, но представлен в виде метода. Часто удобно в цепочках при использовании методичного синтаксиса.

**Пример**

```cpp
String base("Hello");
String result = base.append(", world");  // "Hello, world"
```

*В этом примере текст добавляется к базовой строке.*

---

#### `String prepend(const String &s) const`

**Подпись**

```cpp
String prepend(const String &s) const;
```

**Краткое описание**
Возвращает новую строку с подстрокой, добавленной в начало.

**Параметры**

* `s` – Текст, который будет помещён перед этой строкой.

**Возвращаемое значение**

* Новая `String`, равная `(s + *this)`.

**Детали**

`prepend()` зеркально отражает `append()`, но помещает аргумент перед оригинальной строкой.

**Пример**

```cpp
String name("Alice");
String label = name.prepend("User: ");  // "User: Alice"
```

*В этом примере к имени добавляется префикс.*

---

#### `String insert(int index, const String &s) const`

**Подпись**

```cpp
String insert(int index, const String &s) const;
```

**Краткое описание**
Возвращает новую строку с подстрокой, вставленной по индексу.

**Параметры**

* `index` – Индекс символа, в который вставляется `s`.
* `s` – Текст для вставки.

**Возвращаемое значение**

* Новая `String` с `s`, вставленным в исходную строку на указанной позиции символа.

**Детали**

`insert()` — операция на уровне символов:

* `index` измеряется в символах (так же как `length()`).
* Следует передать значение в диапазоне `0`..`length()`. Значения вне диапазона дают поведение, определяемое реализацией.

`insert(0, s)` эквивалентно `s + *this`. `insert(length(), s)` эквивалентно `append(s)`.

**Пример**

```cpp
String base("Hello world");
String updated = base.insert(5, ",");  // "Hello, world"
```

*В этом примере вставляется запятая между словами.*

---

#### `String remove(int start, int end) const`

**Подпись**

```cpp
String remove(int start, int end) const;
```

**Краткое описание**
Возвращает новую строку с удалёнными символами от start до end.

**Параметры**

* `start` – Индекс символа первого удаляемого символа.
* `end` – Индекс символа **после** последнего удаляемого (исключающий).

**Возвращаемое значение**

* Новая `String` с указанным диапазоном символов удалённым.

**Детали**

`remove()` работает на уровне символов и возвращает новую строку, пропуская указанный диапазон. По сути эквивалентно:

```cpp
substring(0, start) + substring(end)
```

Индексы должны ссылаться на валидные позиции символов внутри строки.

**Пример**

```cpp
String text("Hello, world");
String noComma = text.remove(5, 6);  // "Hello world"
```

*В этом примере удаляется один символ (запятая) из строки.*

---

#### `String replace(const String &target, const String &replacement) const`

**Подпись**

```cpp
String replace(const String &target, const String &replacement) const;
```

**Краткое описание**
Возвращает новую строку со всеми вхождениями target заменёнными.

**Параметры**

* `target` – Подстрока для поиска.
* `replacement` – Текст для подстановки вместо каждого вхождения `target`.

**Возвращаемое значение**

* Новая `String`, где каждая неперекрывающаяся встреча `target` заменена на `replacement`.

**Детали**

`replace()` выполняет глобальную, неперекрывающуюся замену. Он не использует регулярные выражения. Если `target` пустая, поведение не определено; всегда передавайте непустой `target`.

**Пример**

```cpp
String text("color color");
String american = text.replace("color", "colour");  // "colour colour"
```

*В этом примере все вхождения слова заменяются на другое.*

---

#### `String trim() const`

**Подпись**

```cpp
String trim() const;
```

**Краткое описание**
Возвращает новую строку с удалёнными начальными и конечными пробельными символами.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Новая `String` со снятыми пробелами с обоих концов.

**Детали**

`trim()` удаляет пробельные символы в начале и в конце строки. Точное определение «пробельного символа» зависит от реализации, но обычно включает пробелы, табы и переводы строк.

Внутренние пробелы (между невидимыми символами) не затрагиваются.

**Пример**

```cpp
String raw("   hello  \n");
String cleaned = raw.trim();  // "hello"
```

*В этом примере убираются лишние пробелы вокруг токена.*

---

#### `String upper() const`

**Подпись**

```cpp
String upper() const;
```

**Краткое описание**
Возвращает новую строку, где все символы приведены к верхнему регистру.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Новая `String`, где применимые символы преобразованы в верхний регистр.

**Детали**

`upper()` преобразует буквенные символы в верхний регистр. Для ASCII букв это просто. Для Unicode-букв фактическое поведение зависит от Unicode-поддержки реализации.

Используйте это для нечувствительных к регистру сравнений вместе с `lower()` или `upper()` на обоих операндах.

**Пример**

```cpp
String text("Hello");
String shout = text.upper();  // "HELLO"
```

*В этом примере все буквы преобразуются в верхний регистр.*

---

#### `String lower() const`

**Подпись**

```cpp
String lower() const;
```

**Краткое описание**
Возвращает новую строку, где все символы приведены к нижнему регистру.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Новая `String`, где применимые символы преобразованы в нижний регистр.

**Детали**

`lower()` — противоположность `upper()`. Особенно полезна для простых нечувствительных к регистру сравнений:

```cpp
a.lower().equals(b.lower())
```

**Пример**

```cpp
String text("HeLLo");
String normalized = text.lower();  // "hello"
```

*В этом примере смешанный регистр приводится к нижнему.*

---

#### `String reverse() const`

**Подпись**

```cpp
String reverse() const;
```

**Краткое описание**
Возвращает новую строку с символами в обратном порядке.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Новая `String`, содержащая символы оригинала в обратном порядке.

**Детали**

`reverse()` работает на уровне **символов**, поэтому много-байтовые UTF-8 символы остаются целыми и не разбиваются на байты.

**Пример**

```cpp
String text("abc");
String reversed = text.reverse();  // "cba"
```

*В этом примере порядок символов разворачивается.*

---

#### `String repeat(int count) const`

**Подпись**

```cpp
String repeat(int count) const;
```

**Краткое описание**
Возвращает новую строку, содержащую исходное содержимое, повторённое count раз.

**Параметры**

* `count` – Количество повторов всей строки.

**Возвращаемое значение**

* Новая `String`, равная этой строке, соединённой с самой собой `count` раз.

**Детали**

Если `count` равен:

* `0` – Результат пустая строка.
* `1` – Результат копия оригинала.
* Отрицательное значение – поведение определяется реализацией; передавайте неотрицательные значения.

**Пример**

```cpp
String dot(".");
String ellipsis = dot.repeat(3);  // "..."
```

*В этом примере односимвольная строка повторяется для формирования троеточия.*

---

#### `bool equals(const String &other) const`

**Подпись**

```cpp
bool equals(const String &other) const;
```

**Краткое описание**
Возвращает true, если эта строка равна другой строке.

**Параметры**

* `other` – Строка для сравнения.

**Возвращаемое значение**

* `true`, если обе строки содержат точно одинаковую последовательность байтов; иначе `false`.

**Детали**

`equals()` выполняет точную проверку равенства, чувствительную к регистру. Это эквивалентно `operator==`, но некоторые разработчики предпочитают именованный метод для ясности, особенно при цепочках вызовов или передаче как колбэка.

**Пример**

```cpp
String a("test");
String b("test");
bool same = a.equals(b);  // true
```

*В этом примере сравниваются две строки с помощью метода `equals()`.*

---

#### `int compare(const String &other) const`

**Подпись**

```cpp
int compare(const String &other) const;
```

**Краткое описание**
Сравнивает лексикографически. Возвращает <0, 0 или >0.

**Параметры**

* `other` – Строка для сравнения.

**Возвращаемое значение**

* Отрицательное значение, если `*this` лексикографически меньше `other`.
* `0`, если они равны.
* Положительное значение, если `*this` лексикографически больше `other`.

**Детали**

`compare()` выполняет лексикографическое сравнение на основе базовой байтовой последовательности (или последовательности символов, в зависимости от реализации). Полезен для сортировки и упорядочивания.

**Пример**

```cpp
String a("apple");
String b("banana");

int result = a.compare(b);  // result < 0
```

*В этом примере сравниваются две строки для определения порядка сортировки.*

---

#### `bool operator==(const String &other) const`

**Подпись**

```cpp
bool operator==(const String &other) const;
```

**Краткое описание**
Возвращает true, если эта строка равна другой строке.

**Параметры**

* `other` – Строка для сравнения.

**Возвращаемое значение**

* `true`, если строки равны; иначе `false`.

**Детали**

Этот оператор является идиоматическим сокращением для `equals(other)`. Используется в обычном C++ стиле:

```cpp
if (a == b) { ... }
```

**Пример**

```cpp
String a("hello");
String b("hello");
String c("world");

bool ab = (a == b);  // true
bool ac = (a == c);  // false
```

*В этом примере используется оператор равенства для сравнения строк.*

---

#### `bool operator!=(const String &other) const`

**Подпись**

```cpp
bool operator!=(const String &other) const;
```

**Краткое описание**
Возвращает true, если эта строка не равна другой строке.

**Параметры**

* `other` – Строка для сравнения.

**Возвращаемое значение**

* `true`, если строки не равны; `false`, если они равны.

**Детали**

Просто логическое отрицание `operator==`.

**Пример**

```cpp
String a("hello");
String b("world");

if (a != b) {
  // strings differ
}
```

*В этом примере выполняется ветвление, когда две строки не совпадают.*

---

#### `String operator+(const String &other) const`

**Подпись**

```cpp
String operator+(const String &other) const;
```

**Краткое описание**
Возвращает конкатенацию этой строки и другой строки.

**Параметры**

* `other` – Строка, добавляемая в конец этой строки.

**Возвращаемое значение**

* Новая `String`, содержащая конкатенацию `*this` и `other`.

**Детали**

Этот оператор — основной способ объединять строки. Он не модифицирует ни один из операндов; возвращает новую строку с объединённым текстом.

**Пример**

```cpp
String first("Hello");
String second(" world");
String combined = first + second;  // "Hello world"
```

*В этом примере две строки конкатенируются с помощью оператора `+`.*

---

#### `int hash() const`

**Подпись**

```cpp
int hash() const;
```

**Краткое описание**
Возвращает хеш-код для этой строки.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* Целочисленный хеш-код, полученный из содержимого строки.

**Детали**

Хеш-код пригоден для использования в хеш-таблицах или множествах. Точный алгоритм не специфицирован и может меняться между версиями библиотеки, поэтому не следует сохранять хеш-коды или полагаться на их стабильность.

**Пример**

```cpp
String key("user:42");
int h = key.hash();
// h can be used in a custom hash-based structure
```

*В этом примере вычисляется хеш-код из строкового ключа.*

---

#### `List lines() const`

**Подпись**

```cpp
List lines() const;
```

**Краткое описание**
Разбивает эту строку по переводам строк в список.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* `List` значений `String`, каждое из которых представляет одну строку.

**Детали**

`lines()` разбивает строку на отдельные строки по границам перевода строки. Точное поведение по отношению к `'\r\n'` vs `'\n'` зависит от реализации, но типично — разбиение по `'\n'` и удаление завершающего `'\r'`.

Используйте это при обработке конфигурационных файлов, логов или любого многострочного текста.

**Пример**

```cpp
String text("first line\nsecond line");
List ls = text.lines();
// ls[0] == "first line"
// ls[1] == "second line"
```

*В этом примере многострочная строка разбивается на отдельные строки.*

---

#### `String join(const List &list) const`

**Подпись**

```cpp
String join(const List &list) const;
```

**Краткое описание**
Объединяет элементы списка, используя эту строку как разделитель.

**Параметры**

* `list` – `List` значений для объединения. Каждый элемент преобразуется в `String`.

**Возвращаемое значение**

* Новая `String`, состоящая из всех элементов, соединённых с использованием `*this` в качестве разделителя.

**Детали**

`join()` рассматривает текущую `String` как **разделитель**. Например, `String(", ").join(items)` объединяет элементы с `", "` между ними.

Если `list` пуст, результат — пустая строка. Если в `list` один элемент, разделитель не вставляется.

**Пример**

```cpp
List items;
items.append("a").append("b").append("c");

String sep(", ");
String joined = sep.join(items);  // "a, b, c"
```

*В этом примере элементы списка объединяются с запятой и пробелом в качестве разделителя.*

---

#### `List split(const String &sep, int max = 1) const`

**Подпись**

```cpp
List split(const String &sep, int max = 1) const;
```

**Краткое описание**
Разбивает по разделителю в список. max контролирует максимально допустимое число разбиений.

**Параметры**

* `sep` – Непустой разделитель-подстрока.
* `max` – Максимальное число разбиений. Значение по умолчанию `1` означает «не более одного разбиения», в результате до двух элементов.

**Возвращаемое значение**

* `List` сегментов `String`.

**Детали**

Эта функция разбивает строку на каждое вхождение `sep`, не более `max` раз:

* Если `max == 1` (по умолчанию), вы получите не более двух частей: перед первым `sep` и всё остальное после него.
* Если `max <= 0`, поведение определяется реализацией; передавайте `max >= 1`.

Разделитель не включается в результат.

**Пример**

```cpp
String text("key=value=extra");
List parts = text.split("=", 1);
// parts[0] == "key"
// parts[1] == "value=extra"
```

*В этом примере выполняется разбиение только по первому `=`, оставляя остальную часть строки нетронутой.*

---

#### `List split() const`

**Подпись**

```cpp
List split() const;
```

**Краткое описание**
Разбивает по пробельным символам в список.

**Параметры**

* *(нет)*

**Возвращаемое значение**

* `List` `String` токенов, разделённых одним или более пробельными символами.

**Детали**

Этот удобный перегруженный вариант разбивает строку на токены, разделённые пробелами (пробелы, табы, переводы строк и т.д.). Последовательные пробелы обычно рассматриваются как один разделитель.

Пустые токены (из-за ведущих или замыкающих пробелов) обычно отбрасываются.

**Пример**

```cpp
String text(" one  two   three ");
List tokens = text.split();
// tokens[0] == "one"
// tokens[1] == "two"
// tokens[2] == "three"
```

*В этом примере строка токенизируется по пробельным символам.*

---

#### `String format(const List &list) const`

**Подпись**

```cpp
String format(const List &list) const;
```

**Краткое описание**
Заменяет `{0}`, `{1}` и т.д. элементами списка.

**Параметры**

* `list` – `List`, элементы которого будут подставлены в шаблонную строку.

**Возвращаемое значение**

* Новая `String`, в которой заполнители `{n}` заменены строковыми представлениями соответствующих элементов списка.

**Детали**

Эта функция рассматривает текущую строку как **шаблон** и заменяет заполнители вида `{0}`, `{1}`, `{2}`, … на строковое представление `list[0]`, `list[1]` и т.д.

Пример шаблона:

* Шаблон: `"Hello, {0}! You have {1} messages."`
* Список: `["Alice", 3]`
* Результат: `"Hello, Alice! You have 3 messages."`

Если индекс заполнителя выходит за пределы `list`, поведение определяется реализацией; лучше убедиться, что список содержит все необходимые значения.

**Пример**

```cpp
String tmpl("Hello, {0}! You have {1} messages.");

List args;
args.append("Alice").append(3);

String message = tmpl.format(args);
// "Hello, Alice! You have 3 messages."
```

*В этом примере используются заполнители по индексам для форматирования приветствия.*

---

#### `String format(const Map &map) const`

**Подпись**

```cpp
String format(const Map &map) const;
```

**Краткое описание**
Заменяет заполнители `{key}` значениями из map.

**Параметры**

* `map` – `Map`, предоставляющая значения для именованных заполнителей.

**Возвращаемое значение**

* Новая `String`, в которой заполнители `{key}` заменены строковыми представлениями соответствующих значений из map.

**Детали**

Этот оверлоад поддерживает **именованные заполнители**, что часто нагляднее, чем числовые индексы. Например:

* Шаблон: `"Name: {name}, Age: {age}"`
* Map: `{ "name": "Alice", "age": 30 }`
* Результат: `"Name: Alice, Age: 30"`

Если ключ заполнителя отсутствует в map, поведение определяется реализацией; рекомендуется обеспечить, чтобы map содержил все используемые ключи.

**Пример**

```cpp
String tmpl("Name: {name}, Age: {age}");

Map args;
args.put("name", "Alice").put("age", 30);

String result = tmpl.format(args);
// "Name: Alice, Age: 30"
```

*В этом примере map используется для заполнения именованных заполнителей в шаблоне.*

---

#### `String operator+(const char *lhs, const String &rhs)`

**Подпись**

```cpp
inline String operator+(const char *lhs, const String &rhs) {
  return String(lhs) + rhs;
}
```

**Краткое описание**
Оператор конкатенации строк для естественного синтаксиса: "Hello " + myString

**Параметры**

* `lhs` – C-style UTF-8 строка слева.
* `rhs` – `String` справа.

**Возвращаемое значение**

* Новая `String`, равная `String(lhs).operator+(rhs)`.

**Детали**

Этот свободный (non-member) оператор позволяет писать выражения вроде:

```cpp
"Hello " + myString
```

вместо явного создания `String` слева. Это улучшает читаемость при комбинировании строковых литералов и `String` объектов.

**Пример**

```cpp
String name("Alice");
String greeting = "Hello, " + name;  // "Hello, Alice"
```

*В этом примере используется свободный `operator+` для конкатенации литерала и `String` в естественном порядке.*